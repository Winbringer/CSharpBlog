@model IEnumerable<ДвижокНовостейЗМ.Models.Reply>

<p>   
</p>
<table class="table">
    <tr>
        <th>
           Текст ответа
        </th>
        <th>Дата</th>
        <th>Автор</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Avtor.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit","Reply", new { id=item.Id },null) |           
            @Html.ActionLink("Delete", "Delete", "Reply", new { id=item.Id },null)
        </td>
    </tr>
}

</table>
